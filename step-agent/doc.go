/*

Bitrise StepAgent.

This agent is the connection point to the guest virtual machines of Bitrise.

Accepts a step (a command path) and some environment variables (encoded) as it's arguments.

*/
package main
